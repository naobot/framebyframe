---
import Layout from '../../layouts/Layout.astro';

const { default: Content, frontmatter } = await import('../../content/contact.md');
---

<Layout title={frontmatter.title}>
  <Content />
</Layout>